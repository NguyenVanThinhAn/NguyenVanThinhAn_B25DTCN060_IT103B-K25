let user = {
    name: "skibidi",
    age: 10,
}

/**
 * @param {*} price giá trị truyền vào
 * @returns trả về lợi nhuận
 */

console.log(user["name"]);

console.log(Object.keys(user));
console.log(Object.values(user));
console.table(user);
